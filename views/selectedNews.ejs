<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>실시간 TOP NEWS</title>
    <link rel="stylesheet" href="/css/app.css" />
  </head>
  <body>
    <div class="wrap">
      <main>
        <div class="news-title-box">
          <button>업데이트</button>
          <ul>
            <% for(const news of newsArticles) {%>
            <li><a href="<%= news.source.id %>"><%=news.title %></a></li>
            <% }%>
          </ul>
        </div>
        <div class="news-content-box">
          <h2><%= newsArticles[id].title%></h2>
          <p><%= newsArticles[id].content %></p>
        </div>
      </main>
    </div>
    <script type="module" src="/js/home.js"></script>
  </body>
</html>
